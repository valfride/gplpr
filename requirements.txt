# Versions verified against the author's shared yolov11 environment.
# PyTorch/torchvision are installed separately so the CUDA build can match the host.

numpy==2.0.2
Pillow==11.1.0
PyYAML==6.0.2
tqdm==4.67.3
opencv-python==4.13.0.92
kornia==0.8.2
albumentations==2.0.8
matplotlib==3.9.4
tensorboardX==2.6.4
tensorflow==2.20.0
keras==3.10.0

# Imported by the repository but not present in the exported shared environment.
# Keep these explicit for clean-clone reproducibility.
scikit-image
python-Levenshtein
